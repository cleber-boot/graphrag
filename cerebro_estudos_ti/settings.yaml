### This config file contains required core defaults that must be set, along with a handful of common optional settings.
### For a full list of available settings, see https://microsoft.github.io/graphrag/config/yaml/

### LLM settings ###
## There are a number of settings to tune the threading and token limits for LLM calls - check the docs.

# completion_models:
#   default_completion_model:
#     model_provider: gemini                     # <- MUDADO: Direciona para a nuvem da Google
#     model: gemini-2.5-flash-lite        # <- MUDADO: Formato de rota exigido pelo LiteLLM
#     auth_method: api_key
#     api_key: ${GRAPHRAG_API_KEY}
#     retry:
#       type: exponential_backoff

# embedding_models:
#   default_embedding_model:
#     model_provider: gemini                     # <- MUDADO: Direciona o motor de vetores para a Google
#     model: gemini-embedding-2          # <- MUDADO: Rota oficial de embeddings
#     auth_method: api_key
#     api_key: ${GRAPHRAG_API_KEY}
#     retry:
#       type: exponential_backoff

### Document processing settings ###



### LLM settings ###

completion_models:
  default_completion_model:
    model_provider: openai                     
    model: google/gemini-2.5-flash-lite        
    auth_method: api_key
    api_key: ${GRAPHRAG_API_KEY}
    api_base: https://openrouter.ai/api/v1
    concurrent_requests: 4
    retry:
      type: exponential_backoff

embedding_models:
  default_embedding_model:
    model_provider: openai
    model: google/gemini-embedding-2           
    auth_method: api_key
    api_key: ${GRAPHRAG_API_KEY}
    api_base: https://openrouter.ai/api/v1
    concurrent_requests: 4
    retry:
      type: exponential_backoff

input:
  type: text

chunking:
  type: tokens
  size: 1200
  overlap: 100
  encoding_model: o200k_base

### Storage settings ###

input_storage:
  type: file
  base_dir: "input"

output_storage:
  type: file
  base_dir: "output"

reporting:
  type: file
  base_dir: "logs"

cache:
  type: json
  storage:
    type: file
    base_dir: "cache"
    
vector_store:
  type: lancedb
  db_uri: output/lancedb
  index_schema:
    text_unit_text:
      vector_size: 3072
    entity_description:
      vector_size: 3072
    community_full_content:
      vector_size: 3072

### Workflow settings ###

embed_text:
  embedding_model_id: default_embedding_model
  names:
  - text_unit_text
  - entity_description
  - community_full_content

extract_graph:
  completion_model_id: default_completion_model
  prompt: "prompts/extract_graph.txt"
  entity_types: [organization,person,geo,event]
  max_gleanings: 1

summarize_descriptions:
  completion_model_id: default_completion_model
  prompt: "prompts/summarize_descriptions.txt"
  max_length: 500

extract_graph_nlp:
  text_analyzer:
    extractor_type: regex_english

cluster_graph:
  max_cluster_size: 10

extract_claims:
  enabled: false
  completion_model_id: default_completion_model
  prompt: "prompts/extract_claims.txt"
  description: "Any claims or facts that could be relevant to information discovery."
  max_gleanings: 1

community_reports:
  completion_model_id: default_completion_model
  graph_prompt: "prompts/community_report_graph.txt"
  text_prompt: "prompts/community_report_text.txt"
  max_length: 2000
  max_input_length: 8000

snapshots:
  graphml: false
  embeddings: false

### Query settings ###

local_search:
  completion_model_id: default_completion_model
  embedding_model_id: default_embedding_model
  prompt: "prompts/local_search_system_prompt.txt"

global_search:
  completion_model_id: default_completion_model
  map_prompt: "prompts/global_search_map_system_prompt.txt"
  reduce_prompt: "prompts/global_search_reduce_system_prompt.txt"
  knowledge_prompt: "prompts/global_search_knowledge_system_prompt.txt"

drift_search:
  completion_model_id: default_completion_model
  embedding_model_id: default_embedding_model
  prompt: "prompts/drift_search_system_prompt.txt"
  reduce_prompt: "prompts/drift_search_reduce_prompt.txt"

basic_search:
  completion_model_id: default_completion_model
  embedding_model_id: default_embedding_model
  prompt: "prompts/basic_search_system_prompt.txt"

